# RQ2 tables provenance

Numbers come from `outputs/kaggle/RQ2`. Listed below are cells left as
`.xx`/`xxxx` because no run exists, and cells whose numbers are reported
but come from a run that did not converge.

## Table 5 (tab:rq2_overall)

- Deckard / CodeNet: reported, but the run did not converge (recall 1.000, precision 0.501) -- every pair predicted a clone
- FA-AST+GGNN / AtCoder: reported, but the run did not converge (recall 0.998, precision 0.506) -- every pair predicted a clone
- FA-AST+GMN / AtCoder: reported, but the run did not converge (recall 1.000, precision 0.506) -- every pair predicted a clone

## Table 6 (tab:language)

- none

## Pretrained / UniXcoder columns

Those runs belong to a collaborator, so their cells are copied verbatim
from the paper source (`kaggle/lecture.txt`) rather than recomputed.
