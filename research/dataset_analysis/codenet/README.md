# CodeNet dataset analysis

This directory contains the reproducible hardness and pair-graph leakage
analysis for the external CodeNet clone-pair dataset. The source archive and
dataset report are stored locally under `data/external/codenet/`; generated
tables and figures remain beside the analysis scripts because they are the
canonical inputs to the dataset comparison report.
